<h1>{{title}}</h1>
<p>Welcome to {{title}}</p>

<script src="/socket.io/socket.io.js"></script>
<script>
  var socket = io.connect('/lean-game');
  socket.on('Available Roles', function (data) {
    console.log(data);
    
    socket.emit('my other event', { my: 'data' });
  });
</script>
